<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Confession for <%= name %></title>
  <style>
    body {
      background: <%= (theme === 'hearts') ? '#ffe4e1' : (theme === 'vintage') ? '#f5f5dc' : '#f0fff0' %>;
      font-family: cursive;
      padding: 50px;
      text-align: center;
    }

    .box {
      background: white;
      padding: 20px;
      border-radius: 15px;
      max-width: 600px;
      margin: auto;
      box-shadow: 0 0 15px pink;
    }
  </style>
</head>
<body>
  <div class="box">
    <!-- Your content here -->
  </div>
</body>
</html>
